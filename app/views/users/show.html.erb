<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= render "user_profile" %>
		<% if @user.posts.any? %>
			<%= render "shared/current_user_posts" %>
			<% else %>
				There are no posts.
			<% end %>
	</div>
</div>

