<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h1 id="title">Following</h1>
		<% if @users.any? %>
			<%= render "follows" %>
		<% else %>
		 <p>You follow 0 people.</p>
		<% end %>
	</div>
</div>