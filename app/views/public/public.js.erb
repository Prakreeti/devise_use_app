$('#infinite-scrolling').hide();
$('#posts').append('<%= j render partial: "all_posts", locals: {posts: @posts} %>');

<% if @posts.next_page %>
  $('#infinite-scrolling').html("<%=j will_paginate(@posts) %>");
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
